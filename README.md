# Chroma DB Test with C# and .net 10

Uses Chroma from C# (there is currently only python, typescript and rust clients)

The doc is here : https://docs.trychroma.com/docs/overview/introduction

## Uses Chroma from tryAGI since it support api/v2 of Chroma 
https://github.com/tryAGI/Chroma


## Seems that ChromaDB.Client supports only api/v1 for now so it sems to be not working.
https://github.com/ssone95/ChromaDB.Client
